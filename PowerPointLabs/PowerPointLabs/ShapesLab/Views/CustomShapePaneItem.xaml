<UserControl x:Class="PowerPointLabs.ShapesLab.Views.CustomShapePaneItem"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:PowerPointLabs.SyncLab.Views"
             mc:Ignorable="d" 
             d:DesignHeight="60" d:DesignWidth="200"
             MouseDoubleClick="OnMouseDoubleClick">
    <Grid x:Name="grid" ClipToBounds="True">
        <Grid.ColumnDefinitions>
            <ColumnDefinition x:Name="col1" Width="60" MaxWidth="60"/>
            <ColumnDefinition x:Name="col2" Width="140"/>
        </Grid.ColumnDefinitions>
        <Image x:Name="imageBox" Grid.Column="0" Margin="10,10,0,10" OpacityMask="{DynamicResource {x:Static SystemColors.ControlBrushKey}}" HorizontalAlignment="Left" Width="40" Height="40"/>
        <Canvas x:Name="canvas" Grid.Column="1" >
            <TextBox x:Name="textBox" Margin="0,20,0,20" Width="{Binding ActualWidth, ElementName=canvas}" HorizontalAlignment="Stretch" VerticalAlignment="Center"/>
        </Canvas>
    </Grid>
</UserControl>
